import math
ra=float(input('digite um ângulo:'))
s= math .sin(math.radians(ra))
c=math.cos(math.radians(ra))
tg=math.tan(math.radians(ra))
print('o seno vale {:.4f}, o cosseno vale {:.4f} e a tangente vale {:.4f}'.format(s,c,tg))
